{
  "presets": [
    [
      "@babel/preset-env",
      {
        "modules": false   // ← very important! keeps ES Modules instead of converting to CommonJS
      }
    ],
    "@babel/preset-react"
  ]
}
